/* abcdefg */ // sustituiir con los 7 dígitos de la cédula

#include "../include/mapping.h"

struct _rep_mapping {};

TMapping crearMap(nat M) { return NULL; }

void liberarMap(TMapping map) {}

TMapping asociarEnMap(nat clave, double valor, TMapping map) { return NULL; }

TMapping desasociarEnMap(nat clave, TMapping map) { return NULL; }

bool esClaveEnMap(nat clave, TMapping map) { return false; }

double valorEnMap(nat clave, TMapping map) { return 0; }

bool estaLlenoMap(TMapping map) { return false; }


